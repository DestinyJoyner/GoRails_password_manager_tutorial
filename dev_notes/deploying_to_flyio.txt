- install flyctl (fly control)
    - brew install flyctl
- set up production environment before deploy:
    - rails db:encryption:init
    - EDITOR="code --wait" rails credentials:edit --environment=production
- fly.io requires card info to deploy -> not doing this